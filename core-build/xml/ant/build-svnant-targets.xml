<?xml version="1.0"?>
<project basedir="." default="all">

	<path id="svn.classpath">
		<pathelement location="${shared.lib}/svn/svnClientAdapter.jar" />
		<pathelement location="${shared.lib}/svn/javasvn.jar" />
		<pathelement location="${shared.lib}/svn/svnjavahl.jar" />
		<pathelement location="${shared.lib}/svn/ganymed.jar" />
		<pathelement location="${shared.lib}/svn/svnant.jar" />
	</path>

	<taskdef resource="org/tigris/subversion/svnant/svnantlib.xml" classpathref="svn.classpath" />
	
	<target name="all"/>
</project>